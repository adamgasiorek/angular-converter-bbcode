import { BbcodeConverterPipe } from './bbcode-converter.pipe';

describe('BbcodeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new BbcodeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
